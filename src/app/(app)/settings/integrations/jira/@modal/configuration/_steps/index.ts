import { SelectBoardStep } from "./board";
import { SelectDomainStep } from "./domain";
import { SelectProjectStep } from "./project";

export const steps = {
    domain: SelectDomainStep,
    project: SelectProjectStep,
    board: SelectBoardStep,
};
